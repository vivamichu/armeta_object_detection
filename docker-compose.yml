version: "3.9"

services:
  backend:
    build:
      context: ./backend
    env_file:
      - ./backend/.env.example
    environment:
      TZ: Asia/Almaty
      DI_STORAGE_DIR: /data/storage
    volumes:
      - backend-storage:/data/storage
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
    depends_on:
      - backend
    environment:
      TZ: Asia/Almaty
    ports:
      - "5173:80"

volumes:
  backend-storage:
